// server/src/server.ts

console.log('Waste Management Platform - Backend Starting...');
console.log('TypeScript is working! ✓');
console.log('file changed');

// Type annotations example
const port: number = 5000;
const appName: string = 'Waste Management API';

console.log(`${appName} will run on port ${port}`);
